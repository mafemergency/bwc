Building:
    premake5 vs2015

    /* Win32 or x64.
       Debug or Release */
    msbuild build/bwc.sln /p:Configuration=Release;Platform=x64


Todo:
    - all enums should have hardcoded values
    - ensure sizes and offsets of all structs match bwapi
    - implement a lot of missing functions and functionality
